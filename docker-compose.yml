version: '3.9'

services:
  app:
    build:
      context: .
    ports:
      - 8000:8000
    volumes:
        - ./app:/app
        - ./data:/vol/web

    command: sh -c "python manage.py runserver 0.0.0.0:8000"
    environment:
      - DEBUG=1
      - HOSTS=0.0.0.0,pambostkd.duckdns.org
